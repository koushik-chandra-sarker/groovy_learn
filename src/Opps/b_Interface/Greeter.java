package Opps.b_Interface;
/**
 * -An interface defines a contract that a class needs to conform to.
 * -An interface only defines a list of methods that need to be implemented,
 * -but does not define the methods implementation.
 *
 * -	an interface needs to be declared using the interface keyword
 * -	an interface only defines method signatures
 *
 * */
public interface Greeter {
    void greet(String s);
}
